import 'package:flutter/material.dart';

class JJFavorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('我喜欢的'),
      ),
      body: Center(
        child: Text(
          ''
        ),
      ),
    );
  }
}